import {fizzbuzz} from './FizzBuzz'

function myfunc() {
  // todo
}

describe('myfunc', () => {
  it('should multiple 2 arguments.', () => {
    expect(myfunc(/*something*/)).toBe(/*something*/);
  });
})
